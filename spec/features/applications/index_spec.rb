require 'rails_helper'

RSpec.describe 'The applications index page,' do
  before :all do
  end

  before :each do
  end

  after :all do
  end

  it 'has the partial form for an application'

  it 'the form redirects user to the show page of the new application'

  it 'has a list of all the applications'
end
