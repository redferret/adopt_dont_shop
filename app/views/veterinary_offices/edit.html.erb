<h1>Edit Veterinary Office</h1>
<%= form_with model: @veterinary_office, url: "/veterinary_offices/#{@veterinary_office.id}", method: :patch, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :boarding_services %>
  <%= f.check_box :boarding_services %>

  <%= f.label :max_patient_capacity %>
  <%= f.number_field :max_patient_capacity %>

  <%= f.submit %>
<% end %>
