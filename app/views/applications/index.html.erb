<p id="notice"><%= notice %></p>

<h1>Applications</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @applications.each do |application| %>
      <tr>
        <td><%= application.description %></td>
        <td><%= application.status %></td>
        <td><%= link_to 'Show', application, id: "show_#{application.id}" %></td>
        <td><%= link_to 'Edit', edit_application_path(application), id: "edit_#{application.id}" %></td>
        <td><%= link_to 'Destroy', application, method: :delete, data: { confirm: 'Are you sure?' }, , id: "delete_#{application.id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Application', new_application_path %>
